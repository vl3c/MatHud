"""MatHud CLI - Command-line interface for MatHud automation.

Provides commands for:
- Server management (start/stop/status)
- Test execution (server and client tests)
- Canvas operations (clear, reset, zoom, undo/redo, exec)
- Workspace management (list, save, load, delete)
- AI chat interface
- Screenshot capture
"""

from __future__ import annotations

__version__ = "0.1.0"
