# Packages for CI: requirements.txt without the text-to-speech stack (kokoro pulls in torch,
# transformers and spaCy, several GB) and without geckodriver-autoinstaller, which nothing imports.
# The app starts without kokoro; TTS then reports itself unavailable and its two tests are skipped.
openai==2.8.1
anthropic==0.76.0
flask==3.1.2
flask-session==0.8.0
cachelib==0.13.0
python-dotenv==1.2.1
selenium==4.38.0
requests==2.32.5
types-requests==2.32.4.20250913
mypy==1.11.2
ruff==0.15.0
numpy==2.4.1
click==8.3.0
webdriver-manager==4.0.2
psutil==7.2.2
pytest==8.3.4
